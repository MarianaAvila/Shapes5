from graphics import*
print ("Please input how big you would like the squares size to be")

def draw(squares,color):
    square= 50
    checkersWin = GraphWin("Checkers", square*10,square*10)
    checkersWin.setCoords(0,0,square*10,square*10)
    for j in range (8):
        for i in range (8):
            SQ = Rectangle(Point(square*(j+1),square*(j+1)),
                          Point(square*(i+2),square*(i+2)))






    tile.setFill(Color)














SQ.draw(checkersWin)
           
                        
draw(50)
